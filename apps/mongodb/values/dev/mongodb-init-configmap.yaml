apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-init
  namespace: mongodb-dev
data:
  init-replica.js: |
    // Script mejorado para inicializar el replica set
    // Enfoque conservador: preservar configuraciones existentes cuando sea posible
    
    // Verificar el estado actual del replica set
    print("Verificando el estado actual del replica set...");
    var rsStatus;
    var maxRecoveryAttempts = 10;  // Intentos de auto-recuperación
    var recoveryAttempt = 0;
    var recovered = false;
    
    // Bucle de espera para permitir la auto-recuperación del replica set
    while (recoveryAttempt < maxRecoveryAttempts && !recovered) {
      try {
        rsStatus = rs.status();
        print("Intento " + (recoveryAttempt + 1) + ": Estado del replica set: " + JSON.stringify(rsStatus));
        
        // Si el estado es OK, el replica set se ha recuperado
        if (rsStatus.ok === 1) {
          print("✅ Replica set en buen estado, no es necesario intervenir");
          recovered = true;
          break;
        } else {
          print("⏳ El replica set aún no está listo. Esperando para auto-recuperación...");
          sleep(5000);  // Esperar 5 segundos entre intentos
        }
      } catch (err) {
        print("Intento " + (recoveryAttempt + 1) + ": Error al verificar el estado: " + err);
        
        // Si el error es que el replica set no está iniciado (94) o configuración inválida (93)
        if (err.code === 94 || err.code === 93) {
          print("⏳ Replica set no iniciado o con config inválida. Esperando para auto-recuperación...");
        } else {
          print("⚠️ Error inesperado durante la verificación: " + err);
        }
        
        sleep(5000);  // Esperar 5 segundos entre intentos
      }
      recoveryAttempt++;
    }
    
    // Si el replica set se recuperó por sí solo, no necesitamos hacer nada más
    if (recovered) {
      print("🎉 Replica set recuperado automáticamente. No se requiere intervención.");
      
      // Imprimir miembros del replica set para confirmar
      try {
        var config = rs.conf();
        print("Configuración actual del replica set:");
        printjson(config);
      } catch (e) {
        print("No se pudo obtener la configuración: " + e);
      }
      
      // Salir con éxito
      quit(0);
    }
    
    // Si llegamos aquí, necesitamos intervenir
    print("⚠️ El replica set no se recuperó automáticamente después de " + maxRecoveryAttempts + " intentos.");
    print("Procediendo con intervención manual...");
    
    // Inicializar el replica set solo si es necesario
    try {
      print("Revisando si existe alguna configuración previa...");
      
      // Intentar obtener la configuración actual
      var currentConfig = null;
      try {
        currentConfig = rs.conf();
        print("Configuración actual encontrada:");
        printjson(currentConfig);
      } catch (e) {
        print("No se encontró configuración previa: " + e);
      }
      
      if (currentConfig && currentConfig._id) {
        print("Intentando reconfigurar el replica set existente...");
        
        // Utilizar la misma configuración pero actualizar los hosts si es necesario
        var newConfig = {
          _id: currentConfig._id,
          version: currentConfig.version + 1,
          members: [
            { _id: 0, host: "mongodb-0.mongodb:27017", priority: 1 },
            { _id: 1, host: "mongodb-1.mongodb:27017", priority: 1 },
            { _id: 2, host: "mongodb-2.mongodb:27017", priority: 1 }
          ]
        };
        
        try {
          // Usar replSetReconfig para mantener el mismo ID pero actualizar los hosts
          var reconfigResult = db.adminCommand({ replSetReconfig: newConfig, force: true });
          print("Resultado de reconfiguración: " + JSON.stringify(reconfigResult));
        } catch (reconfigErr) {
          print("Error durante la reconfiguración: " + reconfigErr);
          print("Continuando con inicialización completa...");
        }
      } else {
        // Solo iniciar un nuevo replica set si definitivamente no hay configuración previa
        print("No hay configuración previa. Iniciando nuevo replica set...");
        var initConfig = {
          _id: "rs0",
          members: [
            { _id: 0, host: "mongodb-0.mongodb:27017", priority: 1 },
            { _id: 1, host: "mongodb-1.mongodb:27017", priority: 1 },
            { _id: 2, host: "mongodb-2.mongodb:27017", priority: 1 }
          ]
        };
        
        var initResult = rs.initiate(initConfig);
        print("Resultado de inicialización: " + JSON.stringify(initResult));
      }
    } catch (err) {
      print("Error durante la manipulación del replica set: " + err);
      
      // Si ya está inicializado, intentar reconfigurar con fuerza
      if (err.codeName === "AlreadyInitialized") {
        print("Replica set ya inicializado. Intentando reconfiguración forzada...");
        try {
          var forceConfig = {
            _id: "rs0",  // Podríamos obtener el ID actual si está disponible
            version: 1,
            members: [
              { _id: 0, host: "mongodb-0.mongodb:27017", priority: 1 },
              { _id: 1, host: "mongodb-1.mongodb:27017", priority: 1 },
              { _id: 2, host: "mongodb-2.mongodb:27017", priority: 1 }
            ]
          };
          
          var forceResult = db.adminCommand({ replSetReconfig: forceConfig, force: true });
          print("Resultado de reconfiguración forzada: " + JSON.stringify(forceResult));
        } catch (forceErr) {
          print("Error durante la reconfiguración forzada: " + forceErr);
          quit(1);
        }
      } else {
        print("Error fatal durante la manipulación del replica set: " + err);
        quit(1);
      }
    }
    
    // Esperar para que los cambios se apliquen
    print("Esperando 10 segundos para que se apliquen los cambios...");
    sleep(10000);
    
    // Verificar el estado final
    var finalAttempts = 5;
    for (var i = 0; i < finalAttempts; i++) {
      try {
        var finalStatus = rs.status();
        print("Verificación final " + (i + 1) + ": Estado del replica set: " + JSON.stringify(finalStatus));
        
        if (finalStatus.ok === 1) {
          print("✅ Replica set inicializado correctamente");
          
          // Imprimir miembros para confirmar
          try {
            var members = "";
            finalStatus.members.forEach(function(member) {
              members += "\n  - " + member.name + " (estado: " + member.stateStr + ")";
            });
            print("Miembros del replica set:" + members);
          } catch (e) {
            print("No se pudieron listar los miembros: " + e);
          }
          
          quit(0);
        } else {
          print("⚠️ El replica set aún no está totalmente operativo. Esperando...");
          sleep(5000);
        }
      } catch (finalErr) {
        print("Error al verificar el estado final: " + finalErr);
        sleep(5000);
      }
    }
    
    print("⚠️ No se pudo confirmar que el replica set esté completamente operativo.");
    print("Puede requerir intervención manual adicional o más tiempo para estabilizarse.");
    
    // Aún así intentamos continuar, ya que a veces toma más tiempo para que todos los nodos se sincronicen
    print("Script de inicialización completado, continuando con el proceso...");
