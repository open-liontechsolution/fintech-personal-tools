apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mongodb-prod
  namespace: argocd
spec:
  project: tools
  sources:
    - repoURL: https://mongodb.github.io/helm-charts
      targetRevision: 13.6.4
      chart: mongodb
      helm:
        releaseName: mongodb-prod
      
    - repoURL: https://github.com/open-liontechsolution/fintech-personal-tools.git
      targetRevision: HEAD
      path: apps/mongodb/helm-values/prod
      
  destination:
    server: https://kubernetes.default.svc
    namespace: mongodb-prod
  syncPolicy:
    automated:
      # Desactivamos el prune automático para evitar que ArgoCD elimine recursos que no controla
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
